'use client'

/**
 * Previously configured the Capacitor native shell (status bar, keyboard,
 * splash screen, deep links, push notifications). Now a no-op — the native
 * app is pure SwiftUI.
 */
export function NativeBootstrap() {
  return null
}
